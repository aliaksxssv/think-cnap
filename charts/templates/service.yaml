apiVersion: v1
kind: Service
metadata:
  name: {{ include "think-cnap.fullname" . }}
  labels:
    {{- include "think-cnap.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "think-cnap.selectorLabels" . | nindent 4 }}
